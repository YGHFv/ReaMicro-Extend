# Debug Session: flip-comment-crash

状态：[OPEN]

## 症状
- 阅读页翻页直接闪退。
- 在线段评没有显示。

## 可证伪假设
1. ContentDom 段评注入的 AnnotatedString ranges 或三字段同步导致翻页崩溃。
2. 逐章替换或分页缓存清理导致阅读器状态不一致并崩溃。
3. 当前章节上下文解析失败，段评注入未执行。
4. 段评开关关闭或章节私有缓存为空，注入被正常跳过。
5. 原生评论链构造参数不匹配，注入异常后触发阅读流程故障。

## 证据记录
- 待采集真机翻页崩溃堆栈。
- 待采集 ContentDom 注入、章节上下文、段评缓存相关日志。

## 约束
- 获取运行时证据前不修改业务逻辑。
- 修复后需重新构建、安装并进行真机验证。
- 用户确认修复前保留调试记录和插桩。
